[project]
name = "bee-core"
version = "0.1.0"
description = "Bee brain — Route/Flight runtime and stores"
requires-python = ">=3.11"
dependencies = [
  "bee-hive",
  "bee-logging",
  "openai==2.31.0",
  "python-dotenv==1.0.1",
  "pydantic>=2.8,<3",
  "requests>=2.31.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
bee = "bee_core.cli:main"
