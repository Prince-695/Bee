# ==============================================================================
# 🐝 BEE DESKTOP CONSOLE — CLIENT ENVIRONMENT CONFIGURATION (.env)
# ==============================================================================

# Backend API Base URL (defaults to local sidecar daemon on port 8000)
VITE_API_BASE_URL=http://localhost:8000

# Desktop Node Environment mode ('development' | 'production')
NODE_ENV=development
